<h1>タスクリスト一覧ページ</h1>
<ul>
<% @tasks.each do |task| %>
<li><%= link_to(task.id,"/tasks/#{task.id}") %> :<%= task.status %> &gt; <%= task.content %></li>
<% end %>
</ul>
<%= link_to "タスクの登録はここから",new_task_path %>